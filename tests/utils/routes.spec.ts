import * as ROUTES from '../../lib/utils/routes'

// TODO: Tests
describe('index.ts', () => {
  it('route()', () => {
    expect(1).toEqual(1)
  })

  it('registerRoute()', () => {
    expect(1).toEqual(1)
  })

  it('routeFn()', () => {
    expect(1).toEqual(1)
  })
})
